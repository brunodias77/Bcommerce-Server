# =============================================
# BCOMMERCE MICROSERVICES - ENVIRONMENT VARIABLES
# =============================================
# Copie este arquivo para .env e configure as variáveis com valores reais
# NUNCA commite o arquivo .env com valores reais no repositório
# =============================================

# =============================================
# AUTH SERVICE DATABASE
# =============================================
AUTH_DB_NAME=auth_service_db
AUTH_DB_USER=auth_user
AUTH_DB_PASSWORD=auth_password_2024
AUTH_DB_PORT=5432
AUTH_DB_HOST=localhost

# =============================================
# USER SERVICE DATABASE
# =============================================
USER_DB_NAME=user_db
USER_DB_USER=user_user
USER_DB_PASSWORD=user_password_2024
USER_DB_PORT=5433
USER_DB_HOST=localhost

# =============================================
# CATALOG SERVICE DATABASE
# =============================================
CATALOG_DB_NAME=catalog_db
CATALOG_DB_USER=catalog_user
CATALOG_DB_PASSWORD=catalog_password_2024
CATALOG_DB_PORT=5434
CATALOG_DB_HOST=localhost

# =============================================
# CART SERVICE DATABASE
# =============================================
CART_DB_NAME=cart_db
CART_DB_USER=cart_user
CART_DB_PASSWORD=cart_password_2024
CART_DB_PORT=5435
CART_DB_HOST=localhost

# =============================================
# PROMOTION SERVICE DATABASE
# =============================================
PROMOTION_DB_NAME=promotion_db
PROMOTION_DB_USER=promotion_user
PROMOTION_DB_PASSWORD=promotion_password_2024
PROMOTION_DB_PORT=5436
PROMOTION_DB_HOST=localhost

# =============================================
# ORDER SERVICE DATABASE
# =============================================
ORDER_DB_NAME=order_db
ORDER_DB_USER=order_user
ORDER_DB_PASSWORD=order_password_2024
ORDER_DB_PORT=5437
ORDER_DB_HOST=localhost

# =============================================
# NOTIFICATION SERVICE DATABASE
# =============================================
NOTIFICATION_DB_NAME=notification_db
NOTIFICATION_DB_USER=notification_user
NOTIFICATION_DB_PASSWORD=notification_password_2024
NOTIFICATION_DB_PORT=5438
NOTIFICATION_DB_HOST=localhost

# =============================================
# EVENT STORE SERVICE DATABASE
# =============================================
EVENTS_DB_NAME=events_db
EVENTS_DB_USER=events_user
EVENTS_DB_PASSWORD=events_password_2024
EVENTS_DB_PORT=5439
EVENTS_DB_HOST=localhost

# =============================================
# PGADMIN CONFIGURATION
# =============================================
PGADMIN_EMAIL=admin@bcommerce.local
PGADMIN_PASSWORD=admin_password_2024
PGADMIN_PORT=8080

# =============================================
# DOCKER COMPOSE CONFIGURATION
# =============================================
COMPOSE_PROJECT_NAME=bcommerce
COMPOSE_FILE=docker-compose.yml

# =============================================
# SECURITY NOTES
# =============================================
# 1. Altere todas as senhas padrão antes de usar em produção
# 2. Use senhas fortes com pelo menos 16 caracteres
# 3. Considere usar um gerenciador de senhas para gerar senhas seguras
# 4. Em produção, use secrets do Docker Swarm ou Kubernetes
# 5. Configure SSL/TLS para conexões de banco em produção
# 6. Restrinja acesso de rede aos bancos apenas aos serviços necessários
# =============================================

# =============================================
# EXEMPLO DE CONFIGURAÇÃO PARA PRODUÇÃO
# =============================================
# Para produção, considere usar variáveis como:
# AUTH_DB_PASSWORD=SuaS3nh4F0rt3C0mPl3x4!2024
# USER_DB_PASSWORD=0utr4S3nh4Sup3rS3gur4#2024
# CATALOG_DB_PASSWORD=C4t4l0g0P4ssw0rdS3cur3$2024
# ... e assim por diante para todos os serviços
# =============================================